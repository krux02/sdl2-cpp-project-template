#pragma once

// adapt these names, because they can't be changed after a release of the program
#define ORGANIZATION "TemplateFactory"
#define APPLICATION  "SdlTwoTemplate"
#define WIDTH        640
#define HEIGHT       480

#include <string>
extern std::string base_path;
extern std::string pref_path;
